# Secrets
.env

# Intermediate / temp files
tmp/
uploads/
job_outputs/

# Database
*.db
annotate/ls_data/

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
dist/
build/

# macOS
.DS_Store

# Claude Code session state
.claude/
memory/

# Intermediate run outputs (keep only final CSV)
docs/runs/

# Large binary inputs (PDFs/images tracked separately if needed)
docs/*.pdf

# Excel working files
*.xlsx

# Own-system model artifacts (large binaries)
models/*.onnx
models/*.pt
models/paddle_det/
models/paddle_rec/
models/paddle_cls/
runs/
annotate/tiles/

# Stray PyTorch weights at repo root (e.g. yolov8s.pt base weights)
/*.pt

# YOLO dataset images and label caches (regenerated from job_outputs)
datasets/pid_valves/images/
datasets/**/*.cache

# Label Studio export archives (large, regenerable from LS)
annotate/exports/*.zip

# VC / investor data room — kept locally, never pushed
data_room/

# Playwright session data
.playwright-mcp/
themes/screenshots/
# (docker-compose.override.yml is now committed — provides local-dev
#  bind-mounts. Per-developer customization should go in
#  docker-compose.local.yml which IS gitignored:)
docker-compose.local.yml
